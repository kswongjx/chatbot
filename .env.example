# .env.example - Example environment variables
# These are typically used by backend systems or build processes.
# For this static frontend demo, the actual config is placed in config.js,
# which is listed in .gitignore.

# The URL for your n8n webhook that the chat widget will send messages to.
N8N_WEBHOOK_URL=http://localhost:5678/webhook/YOUR_WEBHOOK_ID/chat
